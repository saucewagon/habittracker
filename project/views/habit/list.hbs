<head>
    <link rel="stylesheet" type="text/css" href="css/style.css" />
</head>
<table class="table table-striped">
    <thead>
        <tr>
            <th>Daily Habits</th>
            <th>Check Ins</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {{#each list}}
        <tr>
            <td>{{this.commitment}}</td>
            <td>{{this.checkIns}}</td>
            <td>

<form action="/checkin/{{this._id}}" method="post" autocomplete="off">
    <input type="hidden" name="_id" value="{{habit._id}}">
    <div class="form-group">
        <input type="hidden" class="form-control" name="checkIns" placeholder="Commitment" value="{{this.checkIns}}">
        <input type="submit" value="Check In" />
        </div></form>
                <a href="{{this._id}}">Edit Commitment</a>
                <a href="/delete/{{this._id}}" onclick="return confirm('Confirm delete?');">Delete</a>
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>
<div class="sidebar">This is my daily habit tracking site! Since I'm new to the world of web development, I put this together primarily to learn how Javascript interacts with the client and a database. The tech stack here uses Express, MongoDB, and Handlebars as a template engine. I'm planning on adding a user login system in the future so that habits are individual for each user. I would also like to include some UI visualizations for habit streaks using React. Special thanks to CodAffection for getting me started. 
</div>